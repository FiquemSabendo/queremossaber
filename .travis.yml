language: python
python: 3.6
addons:
  postgresql: "9.5"

install: pip install tox
before_script:
  - psql -c 'CREATE DATABASE lainonima;' -U postgres
  - cp .env.example .env
script:
  - tox
  - make load_fixtures
