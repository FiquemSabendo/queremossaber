language: python
python: 3.6
addons:
  postgresql: "9.5"

install: pip install tox
before_script:
  - psql -c 'CREATE DATABASE lainonima;' -U postgres
  - cp .env.example .env
script:
  # For some reason, "-e ALL" misses the "py36" env
  - tox -e py36,ALL
